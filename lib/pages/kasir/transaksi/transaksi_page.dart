import 'package:flutter/material.dart';

class TransaksiPage extends StatelessWidget {
  static const String id = '/transaksi';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
