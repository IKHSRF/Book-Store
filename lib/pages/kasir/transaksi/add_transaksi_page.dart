import 'package:flutter/material.dart';

class AddTransaksiPage extends StatelessWidget {
  static const String id = '/transaksi/add';
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}