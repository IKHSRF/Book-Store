import 'package:flutter/material.dart';

class AddUserPage extends StatelessWidget {
  static const String id = '/user/add';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
